@page "/chat/{ChannelId}/{Title}"

<ChannelView Title="@Title" ChannelId="@ChannelId" />

@code {
    [Parameter]
    public string Title { get; set; }
    [Parameter]
    public string ChannelId { get; set; }
}
