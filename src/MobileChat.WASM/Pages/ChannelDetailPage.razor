@page "/channeldetail/{ChannelId}/{Title}"

@using MobileChat.UI.Components.View

<ChannelDetailView Title="@Title" ChannelId="@ChannelId" />

@code {
    [Parameter]
    public string Title { get; set; }
    [Parameter]
    public string ChannelId { get; set; }
}