@using BlazorDexie.JsModule
@using Egroo.UI.Helpers
@using Egroo.UI.Constants
@using Microsoft.AspNetCore.Components.Forms
@using Microsoft.AspNetCore.SignalR.Client
@using MudBlazor
@using jihadkhawaja.chat.client
@using jihadkhawaja.chat.client.Services
@using Egroo.UI.CacheDB
@using jihadkhawaja.chat.client.Core

@using jihadkhawaja.chat.shared.Models
@using System.Net.Http.Json
@using Microsoft.JSInterop
@using System.Text.RegularExpressions

@implements IAsyncDisposable
@inherits ProtectedViewBase

@inject SessionStorage SessionStorage
@inject StorageService StorageService
@inject NavigationManager NavigationManager
@inject IUser ChatUserService
@inject ChatMessageService ChatMessageService
@inject IChannel ChatChannelService
@inject ChatCallService ChatCallService
@inject IJSRuntime JSRuntime
@inject ISnackbar Snackbar
@inject BlazorDexie.Options.BlazorDexieOptions BlazorDexieOptions
@inject AgentService AgentService
@inject ChannelFileService ChannelFileService
@inject EndToEndEncryptionService EndToEndEncryptionService
@inject VoiceCallSessionService VoiceCallSession

<MudContainer MaxWidth="MaxWidth.Large" Class="pa-0" Style="height: calc(100vh - 64px); display: flex; flex-direction: column;">
    <!-- Header -->
    <MudPaper Elevation="0" Class="px-4 py-2"
              Style="border-bottom: 1px solid rgba(255,255,255,0.08); flex-shrink: 0; background: var(--mud-palette-surface);">
        <MudStack Row AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
            <MudStack Row AlignItems="AlignItems.Center" Spacing="2">
                <MudIconButton Icon="@Icons.Material.Filled.ArrowBack" Size="Size.Medium" Href="/channels" />
                <MudAvatar Color="Color.Primary" Size="Size.Small">
                    @Title?.ToUpper()[0]
                </MudAvatar>
                <MudStack Spacing="0">
                    <MudText Typo="Typo.body1" Style="font-weight: 600;">@Title</MudText>
                    @if (CallParticipantIds.Count > 0)
                    {
                        <MudText Typo="Typo.caption" Color="Color.Success">
                            @CallParticipantIds.Count in voice call
                        </MudText>
                    }
                </MudStack>
                @if (IsAdmin)
                {
                    <MudIconButton Icon="@Icons.Material.Filled.Edit" Size="Size.Small" Color="Color.Default"
                                   OnClick="EditChannel" />
                }
            </MudStack>
            <MudStack Row AlignItems="AlignItems.Center" Spacing="1">
                <MudTooltip Text="@(IsSearchOpen ? "Close Search" : "Search Messages")">
                    <MudIconButton Icon="@(IsSearchOpen ? Icons.Material.Filled.Close : Icons.Material.Filled.Search)"
                                   Size="Size.Medium"
                                   Color="@(HasActiveSearch ? Color.Primary : Color.Default)"
                                   OnClick="ToggleSearch" />
                </MudTooltip>
                @if (CallParticipantIds.Count > 0)
                {
                    <MudAvatarGroup Max="3" Spacing="2" MaxColor="Color.Primary" MaxSize="Size.Small">
                        @foreach (var participant in CallParticipantUsers.Values.Take(3))
                        {
                            @if (!string.IsNullOrWhiteSpace(participant.AvatarPreview))
                            {
                                <MudTooltip Text="@participant.Username">
                                    <MudAvatar Size="Size.Small">
                                        <MudImage Src="@participant.AvatarPreview" Alt="@participant.Username" />
                                    </MudAvatar>
                                </MudTooltip>
                            }
                            else
                            {
                                <MudTooltip Text="@participant.Username">
                                    <MudAvatar Color="Color.Secondary" Size="Size.Small">
                                        @participant.Username?.ToUpper()[0]
                                    </MudAvatar>
                                </MudTooltip>
                            }
                        }
                    </MudAvatarGroup>
                }
                <MudTooltip Text="@GetCallButtonTooltip()">
                    <MudIconButton Icon="@(IsInCall ? Icons.Material.Filled.CallEnd : Icons.Material.Filled.Call)"
                                   Size="Size.Medium"
                                   Color="@(IsInCall ? Color.Error : VoiceCallSession.IsInCall ? Color.Warning : Color.Default)"
                                   OnClick="OnCallButtonClicked" />
                </MudTooltip>
            </MudStack>
        </MudStack>
        @if (!string.IsNullOrWhiteSpace(EncryptionWarning))
        {
            <MudAlert Severity="Severity.Warning" Dense="true" Variant="Variant.Outlined" Class="mt-2">
                @EncryptionWarning
            </MudAlert>
        }
    </MudPaper>

    <!-- Messages Container -->
    @if (!IsBusy)
    {
        <MudStack Style="flex: 1; min-height: 0; display: flex; flex-direction: column;">
            @if (IsSearchOpen)
            {
                <MudPaper Elevation="0" Class="px-4 py-3"
                          Style="border-bottom: 1px solid rgba(255,255,255,0.08); background: rgba(255,255,255,0.02); flex-shrink: 0;">
                    <MudStack Spacing="2">
                        <MudStack Row AlignItems="AlignItems.Center" Spacing="2">
                            <MudTextField T="string"
                                          Value="SearchQuery"
                                          ValueChanged="OnSearchQueryChangedAsync"
                                          Immediate="true"
                                          Placeholder="Search cached chat history"
                                          Variant="Variant.Outlined"
                                          Margin="Margin.Dense"
                                          Adornment="Adornment.Start"
                                          AdornmentIcon="@Icons.Material.Filled.Search"
                                          Clearable="true"
                                          Class="flex-grow-1" />
                            <MudChip T="string"
                                     Size="Size.Small"
                                     Variant="Variant.Outlined"
                                     Color="@(HasActiveSearch ? Color.Primary : Color.Default)">
                                @GetSearchStatusText()
                            </MudChip>
                        </MudStack>

                        @if (ShouldShowSearchHint())
                        {
                            <MudText Typo="Typo.caption" Style="opacity: 0.75;">
                                Search runs against this device's cached channel history. Type at least @SearchMinimumLength characters.
                            </MudText>
                        }
                        else if (!IsSearchLoading && HasActiveSearch && !SearchResults.Any())
                        {
                            <MudText Typo="Typo.caption" Style="opacity: 0.75;">
                                No cached messages matched your search.
                            </MudText>
                        }

                        @if (IsSearchLoading)
                        {
                            <MudProgressLinear Indeterminate Color="Color.Primary" Class="mt-1" />
                        }
                        else if (SearchResults.Any())
                        {
                            <div style="max-height: 220px; overflow-y: auto; padding-right: 4px;">
                                <MudStack Spacing="1">
                                    @foreach (var result in SearchResults)
                                    {
                                        var searchResultStyle = GetSearchResultStyle(result);
                                        <MudPaper @key="result.ReferenceId"
                                                  Elevation="0"
                                                  Class="pa-3"
                                                  Style="@searchResultStyle"
                                                  @onclick="() => OpenSearchResultAsync(result)">
                                            <MudStack Spacing="1">
                                                <MudStack Row AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
                                                    <MudText Typo="Typo.caption"
                                                             Color="@(result.IsAgent ? Color.Info : result.IsCurrentUser ? Color.Secondary : Color.Primary)"
                                                             Style="font-weight: 600;">
                                                        @result.DisplayName
                                                    </MudText>
                                                    <MudText Typo="Typo.caption" Style="opacity: 0.65;">
                                                        @result.DateLabel
                                                    </MudText>
                                                </MudStack>
                                                <MudText Typo="Typo.body2">
                                                    <MudHighlighter Text="@result.PreviewText"
                                                                    HighlightedText="@SearchQuery"
                                                                    UntilNextBoundary="false"
                                                                    CaseSensitive="false"
                                                                    Markup="false" />
                                                </MudText>
                                            </MudStack>
                                        </MudPaper>
                                    }
                                </MudStack>
                            </div>
                        }
                    </MudStack>
                </MudPaper>
            }
            <div id="messages-container" class="chat-messages-scroll"
                 style="flex: 1; overflow-x: hidden; overflow-y: auto; padding: 16px 24px;">
                @if (!Messages.Any())
                {
                    <MudStack AlignItems="AlignItems.Center" Justify="Justify.Center" Style="height: 100%;">
                        <MudIcon Icon="@Icons.Material.Filled.ChatBubbleOutline" Size="Size.Large"
                                 Style="color: rgba(255,255,255,0.15); font-size: 4rem;" />
                        <MudText Typo="Typo.h6" Style="color: rgba(255,255,255,0.3);">No messages yet</MudText>
                        <MudText Typo="Typo.body2" Style="color: rgba(255,255,255,0.2);">
                            Send a message to start the conversation.
                        </MudText>
                    </MudStack>
                }
                else
                {
                    @foreach (var message in Messages)
                    {
                        <MessageView @key="message.ReferenceId"
                                     DisplayName="@message.DisplayName"
                                     Content="@GetDisplayContent(message)"
                                     Date="@GetDisplayDate(message)"
                                     IsCurrentUser="@(IsCurrentUserMessage(message.SenderId) && !IsAgentMessage(message))"
                                     IsAgent="@IsAgentMessage(message)"
                                     SentOnDate="@message.DateSent"
                                     SeenOnDate="@message.DateSeen"
                                     MessageElementId="@GetMessageElementId(message.ReferenceId)"
                                     IsSearchMatch="@VisibleSearchMatchReferenceIds.Contains(message.ReferenceId)"
                                     IsSearchFocused="@(FocusedSearchReferenceId == message.ReferenceId)" />
                    }
                }
            </div>
            <!-- Input Area -->
            <div style="flex-shrink: 0; padding: 12px 16px 16px; background: var(--mud-palette-background); position: relative;">
                @if (TypingParticipants.Count > 0)
                {
                    <div class="typing-indicator-shell">
                        <MudPaper Class="typing-indicator-bubble" Elevation="0">
                            <div class="typing-indicator-dots" aria-hidden="true">
                                <span></span>
                                <span></span>
                                <span></span>
                            </div>
                            <MudText Typo="Typo.caption">@GetTypingSummary()</MudText>
                        </MudPaper>
                    </div>
                }
                @if (showMentionPopup && filteredMentionCandidates.Count > 0)
                {
                    <MudPaper Class="pa-2 rounded-lg" Elevation="8"
                              Style="position: absolute; bottom: 100%; left: 16px; right: 16px; max-height: 200px; overflow-y: auto; z-index: 10; background: var(--mud-palette-surface); border: 1px solid rgba(255,255,255,0.12);">
                        <MudStack Spacing="0">
                            @for (int i = 0; i < filteredMentionCandidates.Count; i++)
                            {
                                var candidate = filteredMentionCandidates[i];
                                <MudButton OnClick="@(() => SelectMentionCandidate(candidate))"
                                           Variant="Variant.Text" Color="Color.Default" FullWidth
                                           Class="@(i == selectedMentionIndex ? "channel-mention-entry channel-mention-entry-selected" : "channel-mention-entry")">
                                    <MudStack Row AlignItems="AlignItems.Center" Spacing="2">
                                        <MudIcon Icon="@(candidate.IsAgent ? Icons.Material.Filled.SmartToy : Icons.Material.Filled.AlternateEmail)"
                                                 Size="Size.Small"
                                                 Color="@(candidate.IsAgent ? Color.Info : Color.Primary)" />
                                        <MudText Typo="Typo.body2">@candidate.Name</MudText>
                                        @if (!string.IsNullOrWhiteSpace(candidate.Subtitle))
                                        {
                                            <MudText Typo="Typo.caption" Class="channel-mention-subtitle">@candidate.Subtitle</MudText>
                                        }
                                    </MudStack>
                                </MudButton>
                            }
                        </MudStack>
                    </MudPaper>
                }
                <div class="channel-composer-shell">
                    <MudFileUpload @ref="attachmentUpload"
                                   T="IReadOnlyList<IBrowserFile>"
                                   Hidden="true"
                                   Files="pendingAttachmentFiles"
                                   MaximumFileCount="1"
                                   FilesChanged="OnAttachmentFilesChanged">
                        <CustomContent>
                            <MudTooltip Text="Send file">
                                <MudIconButton Icon="@Icons.Material.Filled.AttachFile"
                                               Color="Color.Default"
                                               Size="Size.Medium"
                                               Class="channel-composer-attach"
                                               OnClick="@context.OpenFilePickerAsync"
                                               Disabled="@(InputDisabled || !IsEndToEndReady)" />
                            </MudTooltip>
                        </CustomContent>
                    </MudFileUpload>
                    <MudTextField @ref="inputMudTextField" Underline="false"
                                  @bind-Value="InputContent"
                                  @bind-Value:after="OnInputContentChanged"
                                  Immediate="true"
                                  InputMode="InputMode.text" InputType="InputType.Text"
                                  AutoFocus="false"
                                  Placeholder="Type a message..." Variant="Variant.Text" OnKeyDown="HandleInputKeyDown"
                                  Lines="1" MaxLines="5" MaxLength="MaxMessageLength" Disabled="@(InputDisabled || !IsEndToEndReady)"
                                  Sizing="InputSizing.Auto"
                                  Class="channel-composer-input" />
                    <MudIconButton Icon="@Icons.Material.Filled.Send" Color="Color.Primary"
                                   Size="Size.Medium" Variant="Variant.Filled"
                                   Class="channel-composer-send"
                                   OnClick="SendMessage" ButtonType="ButtonType.Submit"
                                   Disabled="@(InputDisabled || !IsEndToEndReady || !CanSendMessage)" />
                </div>
                @if (stagedAttachment is not null)
                {
                    <MudPaper Class="channel-composer-attachment mt-2 px-3 py-2" Elevation="0">
                        <MudStack Row AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Spacing="2">
                            <MudStack Spacing="0">
                                <MudText Typo="Typo.body2" Style="font-weight: 600;">@stagedAttachment.FileName</MudText>
                                <MudText Typo="Typo.caption" Style="opacity: 0.72;">@stagedAttachment.SummaryText</MudText>
                            </MudStack>
                            <MudIconButton Icon="@Icons.Material.Filled.Close"
                                           Color="Color.Default"
                                           Size="Size.Small"
                                           OnClick="ClearStagedAttachmentAsync"
                                           Disabled="@InputDisabled" />
                        </MudStack>
                    </MudPaper>
                }
            </div>
        </MudStack>
    }
    else
    {
        <MudStack AlignItems="AlignItems.Center" Justify="Justify.Center" Style="flex: 1;">
            <MudProgressCircular Indeterminate Size="Size.Medium" Color="Color.Primary" />
        </MudStack>
    }

</MudContainer>